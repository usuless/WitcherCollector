export type Deck = {
  deck: string;
  card: string;
  location: string;
  image: string;
  id: number;
};

export type Article = {
  color: string;
  title: string;
  description: string;
  image: string;
};
